# 変更履歴

## 論文 v1.5.4 / インタプリタ v8.0.1 — 2026-09-13

別稿 `Generative_Reals_ja_v1.4.1.md` から、現稿の有限構成と計算証跡に合う着想を回収した。内部にZF・ZFC・固定したPA系を加えず、M多項式、Fε、有理区間を返す生成ラベルを別の型として保つ。Fの「〇〇実数」という名称は未確定のままとした。

- Lの加法・順序とQの演算を順序体へ保存する場合の、Fεの条件付き最小性を追加。原案から唯一の解釈が無条件に従うという主張にしない。
- 生成ラベルの規則・パラメータ・仕様・証拠を明示。区間演算、非零分離を証拠とする逆数、条件未充足の式の保持、要求精度の配分を追加。
- 一般交代和、Newton法と二分法の応答整合、誤差率付き数列を有限最大・最小で区間化する定理を追加。別稿の区間過程の商による係数体A、A(t)、Val操作は導入しない。
- 有理精度とFεの全精度を区別し、Fεに平方がεとなる元がないこと、逆数差商の誤差保証、有限長方形和と形式定積分の関係を記述。
- 生成ラベルの値の整数平行移動と逆射を持つ圏、加法群の3手・逆射の5手の条件付き式変形、三レジスタによる加算証跡を追加。一般定理、仮定付きの形式導出、個別実行を区別。
- λライブラリに34定義を追加して全66定義とした。整数は符号・大きさと正の零を維持し、旧差表示との変換を実装。有理数は未約分を許す補助型QRepを使い、零分母を成功値とは別の結果表示で返す。λによるgcd・既約化は未実装。
- インタプリタの評価をδ・βを合わせた左端最外の順序へ変更。新戦略を証跡へ記録し、旧v8.0.0の記録はその版のδ優先戦略で再検査する。
- 生成ラベルと計算モデルの局所証跡、λ整数・QRep算術、改変・型違反・非停止状態の拒否検査を追加。実行結果は検査報告に集約。
- 新定義と6種類の証拠束を含む221構造例、改訂本文の14片、数学実装6ソースのf表記を収録するよう配布資料を更新。手書き証明の全文保存を、その一般証明の形式化とは呼ばない。

9群・157テストの成功を確認。保存済み証拠はλ4例、既存応用6例、Fε5例、生成ラベル14例、レジスタ3例、条件付き等式2例と、Nat/Seqの16一般等式を区別して記録した。

過去の原稿・インタプリタ・原資料を保存し、`output/` に最新ファイルの複製をまとめる。GitHubへのアップロードは行っていない。実ブラウザの画面操作は以前の安全ポリシーによる制約から未実施であり、コードと有限証跡の検査範囲を公開する。

## 論文 v1.5.3 / インタプリタ v8.0.0 — 2026-09-13

著者が採用した体系名と周辺用語を統一した。「〇〇実数」というFの名称は候補検討中とし、この版では確定していない。

- 「f生成体系」「M多項式」「生成複素数」「生成四元数」「数列生成ラベル」「区間生成ラベル」「計算証跡」を本文・概念資料・応用ノート・付録の案内に反映。
- 序盤に用語対応表を追加し、有限記述の範囲、正の無限小を持つFと通常の実数の順序の違い、Fと区間生成ラベルの型の違いを説明。
- 生成ラベルと実行手続きと出力を区別。演算・等値・証明状態は維持。
- 改訂本文に対応するfのみの全文保存を再生成し、復号と既存証拠の再検査を行い、最新版の複製を更新。

## 論文 v1.5.2 / インタプリタ v8.0.0 — 2026-09-13

読者が一つの再帰的な記号から数・演算・証拠の構造が育つことを実感できるよう、fだけの付録を追加し、本文冒頭から案内した。本文はその付録を一段ずつ噛み砕いて説明するものと位置づけた。

- 対 `(s,t)` を `f(E(s)E(t))` に変換。fの直下の子が零・一・二個であることを括弧から一意に読み分け、逆変換できることを証明。
- 整数の二は著者の指定どおり `f(f()f(f(f())))` と表示。M・F・複素数・四元数・λ項・命題・証拠へ同じ変換を適用。
- 付録主部に167件の構造と保存済み4証拠束、後部に本文全文の14片と実行定義4ソースを収録。構造の直接表示と文字列としての全文保存を区別。
- fのみ表記の解析・印刷・相互変換と検査を追加。復元した一般証明と有限計算を元の検査器で再検査し、本文全文は連結後の完全一致を確認。
- 章別対応表と公開用ファイル一式を更新。原資料と過去の論文・概念資料・記号ファイルを保持し、新しい版の記号データは版別のフォルダへ保存。

カンマと対専用の括弧は不要になるが、f自身の範囲を区切る括弧と読取・演算・推論の規則は残る。全文保存を手書き証明の新たな形式化やJavaScript実装の純粋λ化とはしない。

## 論文 v1.5.1 / インタプリタ v8.0.0 — 2026-09-13

著者の指定により、Mを有理係数有限多項式、Fを非零分母のM同士の分数として採用した。旧版の整数段数と有理数の対は比較用Lへ改称し、原資料とv1.5.0は保存している。

- 数・有限列・型・λ項・命題・証明・生成器・解析・射・履歴の構造を、fと括弧とカンマへ戻す規則と網羅的な対応表を追加。
- 原案の整数対としての有理数、複素数の行列表現を保持。四元数を複素数二つの対として構成し、非可換除法環の手書き証明を追加。
- 完全展開した具体例、全内蔵λ定義、既存証拠全体の基本記号ファイルを収録。復号後に元の証拠検査器で再検査する手順を追加。
- 厳密な基本記号パーサと型別コーデック、拒否・往復検査を追加。構文の保存、操作の実行、命題の証明を区別。
- `embedL` を旧一次表示用のAPI名とし、`embedM` と既存証跡の操作名を互換用に保持。Mにはε/2が入るため、整数段数の隣接性がM全体には移らないことを明記。
- 最新版の複製に、概念資料・完全な基本記号ファイル・検査報告を含めるよう更新。

λインタプリタのコードは変更していない。新資料の基本記号列をそのままHTMLへ入力して実行する機能は追加していない。

## 論文 v1.5.0 / インタプリタ v8.0.0 — 2026-09-13

最初の監査・厳密化版。著者の txt、過去モデルの md、旧 HTML を区別して検討し、原資料を変更せずに新しい版を作成した。

- `f()` による生成、体系内の数、λ 表現、型付き等式証明の関係と信頼する規則を明記。
- 自然数・整数・有理数・M の構成と基本的な代数構造を整理。零の正規化、非零分母、等値と演算の整合性を明示。
- `qlim` の有限応答過程と隣接記号の意味を区別。M 同士の分数の不閉性と、多項式比への拡張を明示。全域の標準部の主張を修正。
- qlim の有限段階の証拠と、候補モデル Fε の多項式gcd・四則・順序・有限部分の標準部の実装および計算証拠を追加。
- 自然数13命題・列連結3命題について、帰納法を含む検査可能な有限証明を追加。
- 有限履歴から射のみの圏を構成し、有限精度の √2・Leibniz 列・区間演算・多項式の解析を追加。
- λ インタプリタを束縛位置による内部表現へ改訂。定義展開・β 履歴の保存と別の置換手順による再検証、終了状態の区別を追加。
- 回帰検査、具体的な証拠ファイル、再現スクリプト、最新版の複製とチェックサム一覧を追加。

全ての一般定理をλ履歴だけで機械検証した版ではない。証明状態と未確定の解釈は本文・監査報告に記載。実ブラウザでの画面操作確認は、ブラウザの安全ポリシーがローカルファイル表示を拒否したため未実施。
